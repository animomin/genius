<%

	Dim PathMain, PathContent, PathLib, PathSrc, PathView		' as string	'메인폴더경로
	Dim PathC_css, PathC_img, PathC_inc, PathC_js				' as string 'content 하위 폴더 경로
	Dim PathL_css, PathL_fonst, PathL_js						' as string 'lib 하위 폴더 경로
	Dim PathS_controller, PathS_lib								' as string	'src 하위 폴더 경로
	
	
	
%>